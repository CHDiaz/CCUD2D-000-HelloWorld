using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Challenge1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // DUring the final section on the video, we were challenged to have the console print out our name instead of "Hello World"
        print("CurlyHairedHero says Hi.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
